import { ShadingFunctionEntry } from '../types';

export const SHADING_DICTIONARY: ShadingFunctionEntry[] = [
  // --- Math & Arithmetic ---
  {
    id: 'mix_lerp',
    name: 'Linear Interpolation (mix / lerp)',
    category: 'Math',
    glsl: 'mix(x, y, a)',
    hlsl: 'lerp(x, y, a)',
    cg: 'lerp(x, y, a)',
    wgsl: 'mix(x, y, a)',
    msl: 'mix(x, y, a)',
    description: 'Computes linear interpolation x * (1 - a) + y * a between two values or vectors.',
    parameters: 'x: start value/vector, y: end value/vector, a: weight factor (0.0 to 1.0)',
    returnType: 'Same as x (float, float2, float3, float4)',
    exampleGlsl: 'vec3 col = mix(vec3(0.0), vec3(1.0), 0.5);',
    exampleHlsl: 'float3 col = lerp(float3(0,0,0), float3(1,1,1), 0.5);',
    notes: 'In HLSL, lerp() is hardware-accelerated on all GPUs with fused multiply-add (FMA).',
    mobileCaveats: 'Mobile GPUs (Apple Silicon, Mali, Adreno) execute lerp in 1 cycle using FMA instructions.',
  },
  {
    id: 'fract_frac',
    name: 'Fractional Part (fract / frac)',
    category: 'Math',
    glsl: 'fract(x)',
    hlsl: 'frac(x)',
    cg: 'frac(x)',
    wgsl: 'fract(x)',
    msl: 'fract(x)',
    description: 'Returns the fractional part of x: x - floor(x).',
    parameters: 'x: floating point scalar or vector',
    returnType: 'Same as x',
    exampleGlsl: 'vec2 grid = fract(uv * 10.0);',
    exampleHlsl: 'float2 grid = frac(uv * 10.0);',
    notes: 'Crucial for procedural textures, tiling UV coordinates, and repeating patterns.',
    mobileCaveats: 'Always returns a positive value in [0.0, 1.0) for both positive and negative inputs in GLSL/HLSL.',
  },
  {
    id: 'mod_fmod',
    name: 'Modulus / Remainder (mod / fmod)',
    category: 'Math',
    glsl: 'mod(x, y)',
    hlsl: 'fmod(x, y) or (x - y * floor(x / y))',
    cg: 'fmod(x, y)',
    wgsl: 'x % y',
    msl: 'fmod(x, y)',
    description: 'Calculates the modulus or remainder of x divided by y.',
    parameters: 'x: dividend, y: divisor',
    returnType: 'Same as x',
    exampleGlsl: 'float val = mod(time, 2.0);',
    exampleHlsl: 'float val = x - y * floor(x / y); // exact GLSL mod in HLSL',
    notes: 'CRITICAL DIFFERENCE: GLSL mod() uses floor() (Euclidean, smooth for negative numbers). HLSL fmod() uses truncation towards zero. For exact GLSL behavior in HLSL, use (x - y * floor(x / y)) or frac(x / y) * y.',
    mobileCaveats: 'fmod() can cause negative pattern flipping on mobile if coordinate inputs pass below zero.',
  },
  {
    id: 'inversesqrt_rsqrt',
    name: 'Reciprocal Square Root (inversesqrt / rsqrt)',
    category: 'Math',
    glsl: 'inversesqrt(x)',
    hlsl: 'rsqrt(x)',
    cg: 'rsqrt(x)',
    wgsl: 'inverseSqrt(x)',
    msl: 'rsqrt(x)',
    description: 'Computes 1.0 / sqrt(x) using dedicated GPU reciprocal square root hardware.',
    parameters: 'x: scalar or vector (must be > 0)',
    returnType: 'Same as x',
    exampleGlsl: 'float invLen = inversesqrt(dot(v, v));',
    exampleHlsl: 'float invLen = rsqrt(dot(v, v));',
    notes: 'Significantly faster on GPU than computing 1.0 / sqrt(x). Often used inside normalize() algorithms.',
    mobileCaveats: 'Direct single-cycle instruction on all mobile architectures (ARM Mali, Qualcomm Adreno, PowerVR).',
  },
  {
    id: 'clamp_saturate',
    name: 'Clamp / Saturate Range',
    category: 'Math',
    glsl: 'clamp(x, minVal, maxVal)',
    hlsl: 'clamp(x, minVal, maxVal) / saturate(x)',
    cg: 'clamp(x, minVal, maxVal) / saturate(x)',
    wgsl: 'clamp(x, minVal, maxVal) / saturate(x)',
    msl: 'clamp(x, minVal, maxVal) / saturate(x)',
    description: 'Constrains a value to lie between a minimum and maximum range. HLSL saturate(x) clamps specifically to [0.0, 1.0].',
    parameters: 'x: value, minVal: lower bound (0.0 for saturate), maxVal: upper bound (1.0 for saturate)',
    returnType: 'Same as x',
    exampleGlsl: 'vec3 c = clamp(rawColor, 0.0, 1.0);',
    exampleHlsl: 'float3 c = saturate(rawColor); // 0-cost modifier in HLSL!',
    notes: 'HLSL saturate(x) is completely free on GPUs because output registers support zero-cost [0,1] saturation modifiers.',
    mobileCaveats: 'Use saturate() wherever possible in HLSL instead of clamp(x, 0.0, 1.0) for free ALU cycles.',
  },
  {
    id: 'smoothstep',
    name: 'Hermite Smooth Interpolation (smoothstep)',
    category: 'Math',
    glsl: 'smoothstep(edge0, edge1, x)',
    hlsl: 'smoothstep(edge0, edge1, x)',
    cg: 'smoothstep(edge0, edge1, x)',
    wgsl: 'smoothstep(edge0, edge1, x)',
    msl: 'smoothstep(edge0, edge1, x)',
    description: 'Performs smooth Hermite interpolation between 0.0 and 1.0 when edge0 < x < edge1 with zero 1st derivatives at boundaries.',
    parameters: 'edge0: lower edge, edge1: upper edge, x: source value',
    returnType: 'Same as x',
    exampleGlsl: 'float mask = smoothstep(0.4, 0.6, uv.x);',
    exampleHlsl: 'float mask = smoothstep(0.4, 0.6, uv.x);',
    notes: 'Identical syntax across GLSL and HLSL. Formula: t = clamp((x - edge0)/(edge1 - edge0), 0.0, 1.0); return t*t*(3.0 - 2.0*t);',
    mobileCaveats: 'If edge0 == edge1, leads to division by zero NaN on Mali GPUs.',
  },
  {
    id: 'atan_atan2',
    name: 'Arc Tangent / Angle (atan / atan2)',
    category: 'Trigonometry',
    glsl: 'atan(y, x) or atan(x)',
    hlsl: 'atan2(y, x) or atan(x)',
    cg: 'atan2(y, x) or atan(x)',
    wgsl: 'atan2(y, x) or atan(x)',
    msl: 'atan2(y, x) or atan(x)',
    description: 'Computes the arc tangent of y/x using the signs of both arguments to determine the correct quadrant [-PI, PI].',
    parameters: 'y: numerator coordinate, x: denominator coordinate',
    returnType: 'Float angle in radians',
    exampleGlsl: 'float angle = atan(dir.y, dir.x);',
    exampleHlsl: 'float angle = atan2(dir.y, dir.x);',
    notes: 'GLSL allows 2-argument atan(y, x). In HLSL, you MUST use atan2(y, x) for 2 arguments and atan(x) for single argument.',
    mobileCaveats: 'atan2 is computed using CORDIC or polynomial approximation on mobile; can cost 8-16 cycles.',
  },

  // --- Vectors & Geometry ---
  {
    id: 'dot_product',
    name: 'Dot Product (dot)',
    category: 'Vectors',
    glsl: 'dot(x, y)',
    hlsl: 'dot(x, y)',
    cg: 'dot(x, y)',
    wgsl: 'dot(x, y)',
    msl: 'dot(x, y)',
    description: 'Computes scalar dot product of two vectors: x.x*y.x + x.y*y.y + ...',
    parameters: 'x: vector A, y: vector B',
    returnType: 'float (scalar)',
    exampleGlsl: 'float NdotL = max(0.0, dot(normal, lightDir));',
    exampleHlsl: 'float NdotL = saturate(dot(normalWS, lightDirWS));',
    notes: 'Fundamental building block for Lambertian lighting, distance projections, and specular highlights.',
    mobileCaveats: 'DP3 / DP4 instructions are native single-cycle on all mobile GPUs.',
  },
  {
    id: 'cross_product',
    name: 'Cross Product (cross)',
    category: 'Vectors',
    glsl: 'cross(x, y)',
    hlsl: 'cross(x, y)',
    cg: 'cross(x, y)',
    wgsl: 'cross(x, y)',
    msl: 'cross(x, y)',
    description: 'Computes the vector cross product of two 3D vectors producing an orthogonal normal vector.',
    parameters: 'x: 3D vector A, y: 3D vector B',
    returnType: 'float3 / vec3',
    exampleGlsl: 'vec3 bitangent = cross(normal, tangent.xyz) * tangent.w;',
    exampleHlsl: 'float3 bitangentWS = cross(normalWS, tangentWS.xyz) * tangentWS.w;',
    notes: 'Only defined for 3-component vectors in all shading languages.',
    mobileCaveats: 'Requires 2 cycles of vector ALU instructions.',
  },
  {
    id: 'normalize',
    name: 'Vector Normalization (normalize)',
    category: 'Vectors',
    glsl: 'normalize(v)',
    hlsl: 'normalize(v)',
    cg: 'normalize(v)',
    wgsl: 'normalize(v)',
    msl: 'normalize(v)',
    description: 'Returns a vector in the same direction as v with unit length (magnitude of 1.0).',
    parameters: 'v: vector',
    returnType: 'Same as v (normalized)',
    exampleGlsl: 'vec3 n = normalize(vNormal);',
    exampleHlsl: 'float3 n = normalize(input.normalWS);',
    notes: 'Computes v * rsqrt(dot(v, v)). If length is 0, results in NaN/Inf in some drivers.',
    mobileCaveats: 'Use SafeNormalize() from Unity URP Core.hlsl to prevent zero-vector NaNs in mobile lighting passes.',
  },
  {
    id: 'reflect',
    name: 'Vector Reflection (reflect)',
    category: 'Vectors',
    glsl: 'reflect(I, N)',
    hlsl: 'reflect(I, N)',
    cg: 'reflect(I, N)',
    wgsl: 'reflect(I, N)',
    msl: 'reflect(I, N)',
    description: 'Calculates the reflection vector for incident vector I and surface normal N: I - 2.0 * dot(N, I) * N.',
    parameters: 'I: incident ray vector, N: surface normal (must be normalized)',
    returnType: 'Same vector type as I',
    exampleGlsl: 'vec3 refl = reflect(viewDir, normal);',
    exampleHlsl: 'float3 refl = reflect(viewDirWS, normalWS);',
    notes: 'Incident vector points TOWARDS the surface in standard convention.',
    mobileCaveats: 'Ensure normal is normalized, otherwise reflected ray length will be distorted.',
  },
  {
    id: 'refract',
    name: 'Vector Refraction (refract)',
    category: 'Vectors',
    glsl: 'refract(I, N, eta)',
    hlsl: 'refract(I, N, eta)',
    cg: 'refract(I, N, eta)',
    wgsl: 'refract(I, N, eta)',
    msl: 'refract(I, N, eta)',
    description: 'Computes Snell refraction direction vector given index of refraction ratio eta.',
    parameters: 'I: incident vector, N: surface normal, eta: ratio of indices of refraction (n1/n2)',
    returnType: 'Same vector type as I (returns 0 if total internal reflection occurs)',
    exampleGlsl: 'vec3 refr = refract(viewDir, normal, 1.0 / 1.33);',
    exampleHlsl: 'float3 refr = refract(viewDirWS, normalWS, 1.0 / 1.33);',
    notes: 'Useful for glass, water caustics, and gem refraction shaders.',
    mobileCaveats: 'Check for total internal reflection when ray enters a denser medium at shallow angles.',
  },

  // --- Matrices ---
  {
    id: 'matrix_multiplication',
    name: 'Matrix Multiplication (* vs mul)',
    category: 'Matrices',
    glsl: 'mat4 * vec4 or mat4 * mat4',
    hlsl: 'mul(mat4, vec4) or mul(vec4, mat4)',
    cg: 'mul(mat, vec)',
    wgsl: 'mat * vec',
    msl: 'mat * vec',
    description: 'Multiplies a matrix by a vector or another matrix.',
    parameters: 'M: matrix, v: vector',
    returnType: 'Transformed vector or matrix',
    exampleGlsl: 'vec4 clipPos = u_MVP * vec4(position, 1.0);',
    exampleHlsl: 'float4 clipPos = mul(unity_MatrixVP, float4(posWS, 1.0));',
    notes: 'CRITICAL: In GLSL, * is column-major vector on the right (M * v). In HLSL, mul(M, v) treats v as a column vector; mul(v, M) treats v as a row vector. Unity default shader matrices are column-major in CBUFFER.',
    mobileCaveats: 'Unity SRP handles matrix transformations via TransformObjectToHClip() and TransformObjectToWorld() macros.',
  },
  {
    id: 'transpose',
    name: 'Matrix Transpose (transpose)',
    category: 'Matrices',
    glsl: 'transpose(m)',
    hlsl: 'transpose(m)',
    cg: 'transpose(m)',
    wgsl: 'transpose(m)',
    msl: 'transpose(m)',
    description: 'Transposes matrix m (swaps rows and columns).',
    parameters: 'm: 2x2, 3x3, or 4x4 matrix',
    returnType: 'Transposed matrix',
    exampleGlsl: 'mat3 normalMat = transpose(inverse(mat3(u_Model)));',
    exampleHlsl: 'float3x3 normalMat = transpose((float3x3)unity_WorldToObject);',
    notes: 'In Unity, world-to-object normal transformation is handled automatically via GetVertexNormalInputs.',
    mobileCaveats: 'Matrix inversion in fragment shaders is extremely expensive on mobile. Calculate on CPU or vertex shader.',
  },

  // --- Texturing & Samplers ---
  {
    id: 'texture_sample',
    name: 'Texture Sampling (texture / SAMPLE_TEXTURE2D)',
    category: 'Texturing',
    glsl: 'texture(sampler2D, uv) or texture2D(sampler2D, uv)',
    hlsl: 'SAMPLE_TEXTURE2D(tex, sampler_tex, uv) or tex.Sample(sampler_tex, uv)',
    cg: 'tex2D(tex, uv)',
    wgsl: 'textureSample(t, s, uv)',
    msl: 't.sample(s, uv)',
    description: 'Samples a 2D texture using standard trilinear/anisotropic filtering at given UV coordinates.',
    parameters: 'tex: texture resource, sampler: sampler state, uv: float2 coordinates',
    returnType: 'float4 color (RGBA)',
    exampleGlsl: 'vec4 albedo = texture(_MainTex, uv);',
    exampleHlsl: 'float4 albedo = SAMPLE_TEXTURE2D(_BaseMap, sampler_BaseMap, input.uv);',
    notes: 'Unity SRP uses separated Texture2D and SamplerState objects for DirectX 11/12, Vulkan, and Metal compliance. Declared with TEXTURE2D(name) and SAMPLER(sampler_name).',
    mobileCaveats: 'Texture cache misses cause pipeline stalls on mobile. Group texture reads at the top of fragment shaders.',
  },
  {
    id: 'texture_lod',
    name: 'Texture Sampling with Explicit LOD (textureLod / SAMPLE_TEXTURE2D_LOD)',
    category: 'Texturing',
    glsl: 'textureLod(sampler, uv, lod)',
    hlsl: 'SAMPLE_TEXTURE2D_LOD(tex, sampler_tex, uv, lod)',
    cg: 'tex2Dlod(tex, float4(uv, 0, lod))',
    wgsl: 'textureSampleLevel(t, s, uv, lod)',
    msl: 't.sample(s, uv, level(lod))',
    description: 'Samples a texture at an exact MIP level, avoiding automatic derivative hardware calculation.',
    parameters: 'tex: texture, sampler: sampler state, uv: coordinates, lod: mipmap level (0.0 = full res)',
    returnType: 'float4 color',
    exampleGlsl: 'vec4 blurred = textureLod(_EnvMap, uv, 4.0);',
    exampleHlsl: 'float4 blurred = SAMPLE_TEXTURE2D_LOD(_BaseMap, sampler_BaseMap, uv, 4.0);',
    notes: 'Mandatory when sampling textures inside loops with dynamic branch divergence or in vertex/compute shaders where screen derivatives are unavailable.',
    mobileCaveats: 'Essential for raymarchers and custom blurs on mobile GPUs.',
  },
  {
    id: 'texture_grad',
    name: 'Texture Sampling with Explicit Gradients (textureGrad / SAMPLE_TEXTURE2D_GRAD)',
    category: 'Texturing',
    glsl: 'textureGrad(sampler, uv, dPdx, dPdy)',
    hlsl: 'SAMPLE_TEXTURE2D_GRAD(tex, sampler_tex, uv, ddx, ddy)',
    cg: 'tex2Dgrad(tex, uv, ddx, ddy)',
    wgsl: 'textureSampleGrad(t, s, uv, ddx, ddy)',
    msl: 't.sample(s, uv, gradient2d(ddx, ddy))',
    description: 'Samples a texture with user-supplied screen-space partial derivatives for anisotropic filtering.',
    parameters: 'tex: texture, sampler: sampler, uv: coord, ddx: du/dx, ddy: du/dy',
    returnType: 'float4 color',
    exampleGlsl: 'vec4 col = textureGrad(tex, uv, dFdx(uv), dFdy(uv));',
    exampleHlsl: 'float4 col = SAMPLE_TEXTURE2D_GRAD(tex, sampler_tex, uv, ddx(uv), ddy(uv));',
    notes: 'Fixes texture sampling seam artifacts in spherical/equirectangular projection maps and triplanar mapping.',
    mobileCaveats: 'Prevents MIP mapping pop artifacts around texture UV wrap boundaries (0.0 <-> 1.0).',
  },

  // --- Screen Space & Derivatives ---
  {
    id: 'derivatives_ddx_ddy',
    name: 'Screen Space Partial Derivatives (dFdx / ddx, dFdy / ddy)',
    category: 'Derivatives',
    glsl: 'dFdx(p), dFdy(p), fwidth(p)',
    hlsl: 'ddx(p), ddy(p), fwidth(p)',
    cg: 'ddx(p), ddy(p), fwidth(p)',
    wgsl: 'dpdx(p), dpdy(p), fwidth(p)',
    msl: 'dfdx(p), dfdy(p), fwidth(p)',
    description: 'Calculates the rate of change of variable p across adjacent pixels in a 2x2 GPU fragment quad.',
    parameters: 'p: any float, float2, float3, or float4 expression',
    returnType: 'Same type as p',
    exampleGlsl: 'vec3 flatNormal = normalize(cross(dFdx(vPos), dFdy(vPos)));',
    exampleHlsl: 'float3 flatNormalWS = normalize(cross(ddx(input.positionWS), ddy(input.positionWS)));',
    notes: 'Used to compute flat face normals in fragment shaders, anti-aliased procedural lines, and custom MIP selection.',
    mobileCaveats: 'All 4 threads in a 2x2 GPU quad must execute derivative instructions together. Divergent branching disables derivatives.',
  },
  {
    id: 'frag_coord',
    name: 'Fragment Screen Position (gl_FragCoord / SV_POSITION)',
    category: 'Unity SRP',
    glsl: 'gl_FragCoord',
    hlsl: 'input.positionCS : SV_POSITION / VPOS',
    cg: 'vpos : VPOS',
    wgsl: '@builtin(position) pos : vec4<f32>',
    msl: 'float4 position [[position]]',
    description: 'Contains window-relative coordinates (x, y, z, 1/w) for the current pixel.',
    parameters: 'None (System Value input)',
    returnType: 'float4 / vec4 (x, y in pixel units, z is depth)',
    exampleGlsl: 'vec2 screenUV = gl_FragCoord.xy / u_Resolution.xy;',
    exampleHlsl: 'float2 screenUV = input.positionCS.xy / _ScreenParams.xy;',
    notes: 'In Unity SRP, input.positionCS.xy contains pixel coordinates (0.5 to screenWidth - 0.5). Use GetNormalizedScreenSpaceUV(input.positionCS) from Core.hlsl for normalized [0, 1] screen UVs.',
    mobileCaveats: 'OpenGL window coordinates start bottom-left; DirectX/Metal window coordinates start top-left. Unity macros normalize this.',
  },

  // --- Bitwise & Packing ---
  {
    id: 'pack_unpack_unorm',
    name: 'Color / Vector Packing (packUnorm4x8 / PackFloat4ToUint)',
    category: 'Bitwise',
    glsl: 'packUnorm4x8(v) / unpackUnorm4x8(u)',
    hlsl: 'PackFloat4ToUint / UnpackUintToFloat4 or custom bitshift',
    cg: 'EncodeFloatRGBA / DecodeFloatRGBA',
    wgsl: 'pack4x8unorm(v) / unpack4x8unorm(u)',
    msl: 'pack_float_to_unorm4x8(v) / unpack_unorm4x8_to_float(u)',
    description: 'Packs four 8-bit normalized floating-point values in [0, 1] into a single 32-bit unsigned integer.',
    parameters: 'v: float4 in [0, 1] or u: uint',
    returnType: 'uint or float4',
    exampleGlsl: 'uint packed = packUnorm4x8(vec4(r, g, b, a));',
    exampleHlsl: 'uint packed = (uint(r*255.0) | (uint(g*255.0) << 8) | (uint(b*255.0) << 16) | (uint(a*255.0) << 24));',
    notes: 'Crucial for G-Buffer packing in deferred renderers and compute buffer bandwidth reduction.',
    mobileCaveats: 'Saves 75% memory bandwidth in mobile compute passes.',
  },

  // --- Compute & Synchronization ---
  {
    id: 'compute_thread_id',
    name: 'Compute Dispatch IDs (gl_GlobalInvocationID / SV_DispatchThreadID)',
    category: 'Compute',
    glsl: 'gl_GlobalInvocationID, gl_LocalInvocationID, gl_WorkGroupID',
    hlsl: 'SV_DispatchThreadID, SV_GroupThreadID, SV_GroupID',
    cg: 'N/A',
    wgsl: '@builtin(global_invocation_id) id : vec3<u32>',
    msl: 'uint3 id [[thread_position_in_grid]]',
    description: 'System semantic representing unique global coordinates of the current compute thread.',
    parameters: 'System Value',
    returnType: 'uint3',
    exampleGlsl: 'ivec2 pixel = ivec2(gl_GlobalInvocationID.xy);',
    exampleHlsl: 'uint3 id : SV_DispatchThreadID',
    notes: 'In Unity .compute files, defined in function argument: void CSMain(uint3 id : SV_DispatchThreadID).',
    mobileCaveats: 'Keep thread group dimensions multiples of 32 or 64 (e.g. 8x8x1 or 16x16x1) to match mobile GPU wave/warp size.',
  },
  {
    id: 'memory_barrier',
    name: 'Compute Memory Barrier (memoryBarrier / AllMemoryBarrierWithGroupSync)',
    category: 'Compute',
    glsl: 'memoryBarrier(), barrier(), memoryBarrierShared()',
    hlsl: 'AllMemoryBarrierWithGroupSync(), GroupMemoryBarrierWithGroupSync()',
    cg: 'N/A',
    wgsl: 'workgroupBarrier(), storageBarrier()',
    msl: 'threadgroup_barrier(mem_flags::mem_threadgroup)',
    description: 'Blocks execution of all compute threads in a threadgroup until all memory writes are committed and all threads reach the barrier.',
    parameters: 'None',
    returnType: 'void',
    exampleGlsl: 'barrier(); // sync shared memory in GLSL compute',
    exampleHlsl: 'GroupMemoryBarrierWithGroupSync(); // sync groupshared memory in HLSL',
    notes: 'Critical for GPU parallel reductions, blur kernels, and shared histogram computations.',
    mobileCaveats: 'Avoid excessive barriers inside loops on mobile to prevent warp serialization.',
  },

  // --- Unity SRP Built-in Helpers ---
  {
    id: 'transform_object_to_hclip',
    name: 'Transform Object to Clip Space (TransformObjectToHClip)',
    category: 'Unity SRP',
    glsl: 'u_ModelViewProjectionMatrix * vec4(position, 1.0)',
    hlsl: 'TransformObjectToHClip(positionOS.xyz)',
    cg: 'UnityObjectToClipPos(pos)',
    wgsl: 'TransformObjectToHClip(pos)',
    msl: 'TransformObjectToHClip(pos)',
    description: 'Transforms vertex position from Object Space (OS) directly into Homogeneous Clip Space (HCS). Included from Core.hlsl.',
    parameters: 'positionOS: float3 in object space',
    returnType: 'float4 homogeneous clip coordinates',
    exampleGlsl: 'gl_Position = u_MVP * vec4(a_Position, 1.0);',
    exampleHlsl: 'output.positionCS = TransformObjectToHClip(input.positionOS.xyz);',
    notes: 'The standard vertex transformation in all modern Unity URP and HDRP shaders. Automatically takes SRP Batcher matrix buffers into account.',
    mobileCaveats: 'Faster than manual matrix multiplication because Unity optimizes vertex transforms for SIMD.',
  },
  {
    id: 'transform_object_to_world_normal',
    name: 'Transform Object Normal to World Space (TransformObjectToWorldNormal)',
    category: 'Unity SRP',
    glsl: 'normalize(u_NormalMatrix * normal)',
    hlsl: 'TransformObjectToWorldNormal(normalOS)',
    cg: 'UnityObjectToWorldNormal(norm)',
    wgsl: 'TransformObjectToWorldNormal(norm)',
    msl: 'TransformObjectToWorldNormal(norm)',
    description: 'Correctly transforms surface normal vector from Object Space to World Space, handling non-uniform object scaling correctly via inverse-transpose.',
    parameters: 'normalOS: float3 object space normal',
    returnType: 'float3 normalized world space normal',
    exampleGlsl: 'v_NormalWS = normalize((u_ModelInvTrans * vec4(a_Normal, 0.0)).xyz);',
    exampleHlsl: 'output.normalWS = TransformObjectToWorldNormal(input.normalOS);',
    notes: 'Standard URP Core.hlsl helper.',
    mobileCaveats: 'Avoid normalizing in vertex shader if also normalizing in fragment shader.',
  },
  {
    id: 'sample_scene_depth',
    name: 'Sample Camera Depth Texture (SampleSceneDepth)',
    category: 'Unity SRP',
    glsl: 'texture(u_CameraDepthTexture, screenUV).r',
    hlsl: 'SampleSceneDepth(screenUV) or LinearEyeDepth(SampleSceneDepth(uv), _ZBufferParams)',
    cg: 'LinearEyeDepth(SAMPLE_DEPTH_TEXTURE(_CameraDepthTexture, uv))',
    wgsl: 'SampleSceneDepth(uv)',
    msl: 'SampleSceneDepth(uv)',
    description: 'Samples the scene depth buffer generated by URP depth prepass or opaque pass. Included in DeclareDepthTexture.hlsl.',
    parameters: 'screenUV: float2 normalized screen coordinates [0, 1]',
    returnType: 'float raw depth value (or convert with LinearEyeDepth / Linear01Depth)',
    exampleGlsl: 'float depth = texture(depthTex, screenUV).r;',
    exampleHlsl: 'float rawDepth = SampleSceneDepth(screenUV);\nfloat linearDepth = LinearEyeDepth(rawDepth, _ZBufferParams);',
    notes: 'Handles platform differences between reversed-Z (D3D, Metal, Vulkan [1, 0]) and non-reversed Z (OpenGL [0, 1]).',
    mobileCaveats: 'Requires Depth Texture enabled in URP Asset settings. On mobile tile-based GPUs, depth reads use color attachment fetch.',
  },
  {
    id: 'unity_object_to_clip_pos',
    name: 'Object to Clip Position (UnityObjectToClipPos -> TransformObjectToHClip)',
    category: 'Unity SRP',
    glsl: 'u_MVPMatrix * vec4(position, 1.0)',
    hlsl: 'TransformObjectToHClip(positionOS.xyz)',
    cg: 'UnityObjectToClipPos(v.vertex)',
    wgsl: 'TransformObjectToHClip(pos)',
    msl: 'TransformObjectToHClip(pos)',
    description: 'Transforms object space 3D vertex position to homogeneous clip space (SV_POSITION) in vertex shader.',
    parameters: 'positionOS: float3 object space position',
    returnType: 'float4 homogeneous clip space coordinate',
    exampleGlsl: 'gl_Position = u_ModelViewProjection * vec4(a_Position, 1.0);',
    exampleHlsl: 'output.positionCS = TransformObjectToHClip(input.positionOS.xyz);',
    notes: 'In URP Core.hlsl, TransformObjectToHClip is optimized to use separate world and view-projection transforms for GPU instancing and SRP Batcher caching.',
    mobileCaveats: 'Pre-computed matrix multiplications avoid redundant per-vertex matrix math on mobile vertex engines.',
  },
  {
    id: 'builtin_light_pos_dir',
    name: 'Main Light Direction (_WorldSpaceLightPos0 -> GetMainLight().direction)',
    category: 'Unity SRP',
    glsl: 'u_LightDirection',
    hlsl: 'GetMainLight().direction or _MainLightPosition.xyz',
    cg: '_WorldSpaceLightPos0.xyz',
    wgsl: 'GetMainLight().direction',
    msl: 'GetMainLight().direction',
    description: 'Retrieves the direction vector pointing towards the scene main directional light in world space.',
    parameters: 'None (or pass shadow coordinates for shadow attenuation)',
    returnType: 'float3 normalized world space light direction',
    exampleGlsl: 'vec3 lightDir = normalize(u_LightPos - v_WorldPos);',
    exampleHlsl: 'Light mainLight = GetMainLight();\nfloat3 lightDir = mainLight.direction;',
    notes: 'In Built-in, _WorldSpaceLightPos0 held directional light vectors with w=0 or point light positions with w=1. In URP, use GetMainLight() for main light and GetAdditionalLight(i, posWS) for point/spot lights.',
    mobileCaveats: 'Single-pass forward in URP eliminates multi-pass drawcall overhead of Built-in ForwardAdd passes.',
  },
  {
    id: 'builtin_light_color',
    name: 'Main Light Color (_LightColor0 -> GetMainLight().color)',
    category: 'Unity SRP',
    glsl: 'u_LightColor',
    hlsl: 'GetMainLight().color or _MainLightColor.rgb',
    cg: '_LightColor0.rgb',
    wgsl: 'GetMainLight().color',
    msl: 'GetMainLight().color',
    description: 'Retrieves the RGB color and intensity multiplier of the main scene light.',
    parameters: 'None',
    returnType: 'half3 or float3 linear RGB color',
    exampleGlsl: 'vec3 lightCol = u_LightColor.rgb * u_LightIntensity;',
    exampleHlsl: 'Light mainLight = GetMainLight();\nhalf3 lightColor = mainLight.color;',
    notes: 'In URP, light.color is already pre-multiplied with the light intensity value in linear color space.',
    mobileCaveats: 'Store in half3 on mobile GPUs to conserve register pressure and bandwidth.',
  },
  {
    id: 'builtin_fog_migration',
    name: 'Atmospheric Fog (UNITY_TRANSFER_FOG / UNITY_APPLY_FOG -> MixFog)',
    category: 'Unity SRP',
    glsl: 'mix(fogColor, surfaceColor, fogFactor)',
    hlsl: 'ComputeFogFactor(positionCS.z) and MixFog(color.rgb, fogFactor)',
    cg: 'UNITY_TRANSFER_FOG(o, o.vertex) and UNITY_APPLY_FOG(i.fogCoord, col)',
    wgsl: 'MixFog(col, fogFactor)',
    msl: 'MixFog(col, fogFactor)',
    description: 'Computes fog distance factor in vertex shader and blends scene atmospheric/volumetric fog color in fragment shader.',
    parameters: 'z: float clip space depth, color: half3 surface color, fogFactor: float factor [0, 1]',
    returnType: 'half3 fogged surface color',
    exampleGlsl: 'float fog = clamp((gl_FragCoord.z - fogStart) / (fogEnd - fogStart), 0.0, 1.0);',
    exampleHlsl: '// Vertex: output.fogFactor = ComputeFogFactor(output.positionCS.z);\n// Fragment: finalColor.rgb = MixFog(finalColor.rgb, input.fogFactor);',
    notes: 'Core.hlsl provides ComputeFogFactor and MixFog which automatically handle Linear, Exponential, and Exp2 fog modes configured in Lighting settings.',
    mobileCaveats: 'MixFog executes in 2 arithmetic instructions and works seamlessly with transparent materials.',
  },
  {
    id: 'builtin_object_to_world_matrix',
    name: 'Object to World Matrix (_Object2World / unity_ObjectToWorld -> GetObjectToWorldMatrix())',
    category: 'Unity SRP',
    glsl: 'u_ModelMatrix',
    hlsl: 'GetObjectToWorldMatrix() or TransformObjectToWorld(posOS)',
    cg: 'unity_ObjectToWorld / _Object2World',
    wgsl: 'GetObjectToWorldMatrix()',
    msl: 'GetObjectToWorldMatrix()',
    description: 'Retrieves the 4x4 model/object to world transformation matrix for the current rendered instance.',
    parameters: 'posOS (optional): float3 object space position',
    returnType: 'float4x4 matrix or float3 world position',
    exampleGlsl: 'vec4 worldPos = u_Model * vec4(a_Position, 1.0);',
    exampleHlsl: 'float3 worldPos = TransformObjectToWorld(input.positionOS.xyz);',
    notes: 'Always use GetObjectToWorldMatrix() or TransformObjectToWorld() in SRP instead of directly reading unity_ObjectToWorld to maintain SRP Batcher and GPU Instancing compatibility.',
    mobileCaveats: 'Allows dynamic instancing without uniform array indexing overhead.',
  },
];

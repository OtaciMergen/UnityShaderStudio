# Environment Configuration
# Note: UniShader Studio is 100% deterministic and runs completely offline/client-side.
# No external AI API keys or third-party paid subscriptions are needed.

# Node Server Port (Default: 3000)
PORT=3000

# Environment Mode (development / production)
NODE_ENV=development
